<%= form_for @technology do |f| %>
  <% if @technology.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@technology.errors.count, "error") %> prohibited
        this technology from being saved:
      </h2>
      <ul>
        <% @technology.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>