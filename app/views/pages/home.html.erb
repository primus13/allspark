<h1>Home</h1>

<% if @projects.empty? %>
    <p>No projects found</p>
<% else %>
    <% @projects.each do |project| %>
        <p><%= link_to project.title, project_path(project) %></p>
        <p><%= image_tag project.image.url(:thumb) %></p>
        <hr />
    <% end %>
<% end %>
