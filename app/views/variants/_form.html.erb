<%= form_for @variant do |f| %>
  <% if @variant.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@variant.errors.count, "error") %> prohibited
        this variant from being saved:
      </h2>
      <ul>
        <% @variant.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p><%= f.label :technology %><br>
  	<%= f.collection_select(:technology_id, Technology.all, :id, :name, :prompt => true) %><br>
  </p>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :diameter %><br>
    <%= f.text_area :diameter %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>